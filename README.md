# Dhruval Portfolio (React)

A full React + Vite portfolio rebuild with the original content and a new visual style inspired by the samples in `samples/`.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
